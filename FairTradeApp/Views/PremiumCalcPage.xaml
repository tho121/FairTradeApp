<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:FairTradeApp.ViewModels"
             x:Class="FairTradeApp.Views.PremiumCalcPage">
	<ContentPage.BindingContext>
        <vm:PremiumCalcViewModel />
    </ContentPage.BindingContext>
	
    <ContentPage.Content>
        <StackLayout>
            <Label Text="Premium calculator"
                VerticalOptions="Start" 
                HorizontalOptions="CenterAndExpand" 
                FontSize="Title"/>
			<Label Text="{Binding Premium_res}" Margin="0,10" FontSize="Title" HorizontalTextAlignment="Center"></Label>
			<Label Text="{Binding Premium}" FontFamily="Lobster" HorizontalTextAlignment="Center"/>
			
		</StackLayout>
    </ContentPage.Content>
</ContentPage>